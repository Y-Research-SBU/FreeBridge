"""Setup script for FreeBridge."""

import setuptools

setuptools.setup(
    name="freebridge",
    version="0.1.0",
    description="FreeBridge: empirical-support bridge matching for single-cell latent transition modeling.",
    url="https://github.com/Y-Research-SBU/FreeBridge",
    packages=setuptools.find_packages(),
)
